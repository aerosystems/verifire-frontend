# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "VUE_APP_PRJCT_SERVICE_BASE_URL=${{ secrets.VUE_APP_PRJCT_SERVICE_BASE_URL }}" >> .env
          echo "VUE_APP_SBS_SERVICE_BASE_URL=${{ secrets.VUE_APP_SBS_SERVICE_BASE_URL }}" >> .env
          echo "VUE_APP_RECAPTCHA_V3_SITE_KEY=${{ secrets.VUE_APP_RECAPTCHA_V3_SITE_KEY }}" >> .env
      - run: cat .env
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_VERIFIRE_60D1B }}
          channelId: live
          projectId: verifire-60d1b
